Run "yarn deploy --network polygonMumbai" to deploy
Run "yarn verify --network polygonMumbai" to verify

Note that the Graph Protocol Schema and deployment is located in Repute repo, instead of Repute2 (this repo)